# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: homepage.spec.ts >> 🏠 Homepage & Navigation >> homepage loads with correct title
- Location: tests/homepage.spec.ts:4:7

# Error details

```
Error: expect(page).toHaveTitle(expected) failed

Expected pattern: /HayHome/i
Received string:  "Dispatcher.PRO"
Timeout: 5000ms

Call log:
  - Expect "toHaveTitle" with timeout 5000ms
    14 × unexpected value "Dispatcher.PRO"

```

```yaml
- heading "Dispatcher.PRO" [level=1]
- paragraph: Система управления персоналом
- textbox "Телефон"
- textbox "Пароль"
- button "Показать/скрыть пароль": 👁
- button "Войти"
- text: "Нет аккаунта? Зарегистрироваться Забыли пароль? Кабинеты:"
- link "👨‍💼 Диспетчер":
  - /url: https://xn----gtbdan3bddhceo9d.xn--p1ai
- link "👷 Исполнитель":
  - /url: https://xn----gtbdan3bddhceo9d.xn--p1ai/worker.html
- link "🏢 Клиент":
  - /url: https://xn----gtbdan3bddhceo9d.xn--p1ai/client.html
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | 
  3  | test.describe("🏠 Homepage & Navigation", () => {
  4  |   test("homepage loads with correct title", async ({ page }) => {
  5  |     await page.goto("/");
  6  |     await page.waitForLoadState("networkidle");
> 7  |     await expect(page).toHaveTitle(/HayHome/i);
     |                        ^ Error: expect(page).toHaveTitle(expected) failed
  8  |   });
  9  | 
  10 |   test("logo is visible and links to home", async ({ page }) => {
  11 |     await page.goto("/");
  12 |     await page.waitForLoadState("networkidle");
  13 |     const logo = page.locator("a[href='/']").first();
  14 |     await expect(logo).toBeVisible();
  15 |   });
  16 | 
  17 |   test("header navigation links work", async ({ page }) => {
  18 |     await page.goto("/");
  19 |     await page.waitForLoadState("networkidle");
  20 | 
  21 |     const hostsLink = page.locator('a[href="/hosts"]').first();
  22 |     if (await hostsLink.isVisible({ timeout: 2000 }).catch(() => false)) {
  23 |       await hostsLink.click();
  24 |       await expect(page).toHaveURL(/\/hosts/);
  25 |     }
  26 |   });
  27 | 
  28 |   test("language switcher opens and shows languages", async ({ page }) => {
  29 |     await page.goto("/");
  30 |     await page.waitForLoadState("networkidle");
  31 | 
  32 |     const langBtn = page.locator('button:has(svg), [aria-label*="language" i]').first();
  33 |     if (await langBtn.isVisible({ timeout: 2000 }).catch(() => false)) {
  34 |       await langBtn.click();
  35 |       await page.waitForTimeout(500);
  36 |       const body = await page.textContent("body");
  37 |       expect(body).toMatch(/Русский|English|Հայերեն|Français/);
  38 |     }
  39 |   });
  40 | });
  41 | 
```